# 004 — Qwen3-8B modelopt-QAD w4a4-kv8 step500: GPQA + RULER@8k

- **Date:** 2026-07-01
- **Box:** zhizhousha 4x NVIDIA B200 (sm100)
- **Serving stack:** `/home/zhizhousha/vllm-venv` — vLLM 0.24.0, torch 2.11.0+cu130, flashinfer
- **Eval service:** hosted TURBO-EVAL (`eval.together-turbo.com`) via `run_eval.py`, over cloudflared tunnels
- **Model (HF `togethercomputer/`, `step500` checkpoint):**
  - `Qwen3-8B-qad-w4a4-kv8-exported/step500` — quant_algo **NVFP4 (w4a4)**, KV FP8 (`kv_cache_quant_algo=FP8`)
- **Design:** 1 model x {GPQA, RULER@8k} = 2 experiments (14 jobs), 2 servers (thinking + non-thinking) on 2 GPUs.
- **Motivation:** the only w4a4 QAD student (weights AND activations 4-bit); the 002/003 QAD models are all w4a16. Shows how much is lost when activations are also quantized to 4-bit.

## Settings

Serving (TP=1, `--max-model-len 40960`, `--gpu-memory-utilization 0.85`, `--trust-remote-code`, NO `--enforce-eager`, quant auto `modelopt_fp4`, `--kv-cache-dtype fp8`, distinct `VLLM_PORT` per server):
- Thinking (GPQA): `--override-generation-config {"temperature":0.6,"top_p":0.95,"top_k":20,"min_p":0}`.
- Non-thinking (RULER): `--default-chat-template-kwargs '{"enable_thinking": false}'` + override `{0.7,0.8,20,0}`.

Submission: GPQA `gpqa_diamond` 198, conc 32, max-tokens 32768, T0.6/top_p0.95.
RULER 13 tasks, 50 examples, conc 16, max-tokens 2048, `--max-length 8192 --tokenizer Qwen/Qwen3-8B`, T0.7/top_p0.8.

## Results (all 14 jobs completed, 0 errored; verified against eval service)

### GPQA-Diamond (thinking, 198/198)

| model | GPQA |
| --- | --- |
| Qwen3-8B w4a4-kv8 step500 | **0.5808** |

### RULER @ 8k (non-thinking, 50/task) — acc = `accuracy/8192`

| task | acc |
| --- | --- |
| niah_single_1   | 1.000 |
| niah_single_2   | 1.000 |
| niah_single_3   | 1.000 |
| niah_multikey_1 | 1.000 |
| niah_multikey_2 | 0.980 |
| niah_multikey_3 | 1.000 |
| niah_multivalue | 1.000 |
| niah_multiquery | 1.000 |
| ruler_cwe       | 0.802 |
| ruler_fwe       | 0.880 |
| ruler_vt        | 0.000 |
| ruler_qa_hotpot | 0.580 |
| ruler_qa_squad  | 0.642 |
| **mean (13 tasks)** | **0.837** |

## Cross-run summary (all models so far)

| run | model | GPQA | RULER@8k (13-task mean) |
| --- | --- | --- | --- |
| 001 | Qwen3-8B bf16                    | 0.5808 | 0.795 |
| 001 | Qwen3-8B NVFP4 (w4a4-kv16)       | 0.5051 | 0.775 |
| 002 | w4a4-kvfp8 (QAD student)         | 0.5455 | 0.768 |
| 002 | w4a16-kv16 (QAD student)         | 0.5404 | 0.761 |
| 002 | w4a16-kvfp8-cast (QAD student)   | 0.5505 | 0.777 |
| 003 | w4a16-kv16 step500               | 0.5354 | 0.773 |
| 003 | w4a16-kv8-cast step500           | 0.5202 | 0.814 |
| 004 | **w4a4-kv8 step500**             | **0.5808** | **0.837** |

## Findings / caveats

- **This w4a4-kv8 step500 is the strongest quantized model on BOTH metrics.** Its GPQA (0.5808) *ties bf16* and beats every other quantized variant, and its RULER 13-task mean (0.837) is the highest of any model here. Notable given it's the most aggressive quantization (activations also 4-bit). The QAD training on this checkpoint recovered the w4a4 accuracy gap completely on these two benchmarks — cf. the naive w4a4 NVFP4 in 001 (GPQA 0.505 / RULER 0.775).
- **`niah_multikey_2` = 0.980** here vs 0.18–0.80 for all other models — the main driver of the high RULER mean; long-context multi-key retrieval held up under w4a4.
- **`ruler_vt` = 0.000** again (every model, all runs) — systematic non-thinking task/format artifact, not a quant effect. Kept in the 13-task mean.
- All 14 jobs reached intended totals with 0 errored trials.

## Ops notes

- **Very long warmup for w4a4:** the servers took ~17 min to become ready. `fp4_gemm` autotune alone was ~11 min (21 profiles), followed by inductor compile + FULL cudagraph capture. During autotune GPU util reads 0% (profiling is between-kernel/CPU-bound) and the `\r`-based progress bar can look frozen in `tail` — convert `\r`→`\n` to see real progress. This is normal for w4a4, not a hang.
- Distinct `VLLM_PORT` per server (51000/51001) — avoided the internal torch.distributed port collision seen in 003.
- 14 jobs submitted in one batch, under the 20-jobs/min eval-service rate limit.

## Job ids

Raw run_eval logs + job ids: `~/qwen3-eval-run/eval_logs/w4a4kv8s500__<benchmark>.log`.
